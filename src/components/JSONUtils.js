export function createJSON(entry) {
    console.log("Creating JSON with entry", entry);
}

//TODO: Validation method
// check if: state name two chars (can check immediately on front)
// check if: current day is one day after last entered day
// check if: text is empty
// check if: location falls within specified date range for location